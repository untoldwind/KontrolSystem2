use { Vessel } from ksp::vessel
use { CONSOLE } from ksp::console
use { atmo_launch } from std::atmo

pub fn main_flight(vessel: Vessel, target_apoapsis: int = 100000) -> Result<Unit, string> = {
    atmo_launch(vessel, target_apoapsis, 90)?
}