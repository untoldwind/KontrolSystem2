import { RealizedType, TO2Type } from "./to2-type";

export class RangeType implements RealizedType {
  public name: string = "Range";
  public localName: string = "Range";
  public description: string = "";
}
