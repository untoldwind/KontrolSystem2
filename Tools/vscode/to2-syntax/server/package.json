{
  "name": "to2-lsp-server",
  "description": "Example implementation of a language server in node.",
  "version": "1.0.0",
  "license": "MIT",
  "publisher": "kontrolsystem2",
  "engines": {
    "node": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/untoldwind/KontrolSystem2"
  },
  "dependencies": {
    "vscode-languageserver": "^8.1.0",
    "vscode-languageserver-textdocument": "^1.0.8"
  },
  "devDependencies": {
    "@types/jest": "29.5.1",
    "@types/unicode-properties": "1.3.0",
    "jest": "29.5.0",
    "prettier": "2.8.8",
    "ts-jest": "29.1.0",
    "unicode-properties": "1.4.1",
    "esbuild": "0.17.18",
    "rimraf": "5.0.0"
  },
  "scripts": {
    "bundle": "tsc --noEmit && rimraf out && esbuild src/server.ts --bundle --sourcemap --outdir=out --platform=node",
    "test": "jest",
    "format": "prettier --write ."
  }
}
